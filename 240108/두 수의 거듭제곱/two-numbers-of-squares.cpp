#include <iostream>
#include <cmath>
using namespace std;

int p(int a, int b) {
    return pow(a, b);
}

int main() {
    int a, b;
    cin >> a >> b;
    cout << p(a, b);
    // 여기에 코드를 작성해주세요.
    return 0;
}